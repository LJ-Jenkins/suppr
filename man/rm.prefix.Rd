% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/character.r
\name{rm.prefix}
\alias{rm.prefix}
\alias{rm.suffix}
\title{Remove a prefix or suffix}
\usage{
rm.prefix(x, prefix)

rm.suffix(x, suffix)
}
\arguments{
\item{x}{A character vector.}

\item{prefix, suffix}{A single string.}
}
\value{
\code{x} is returned with the specified prefix or suffix
removed from each element where it was present.
}
\description{
Remove a prefix or suffix from a character vector.
}
\details{
Elements without the specified prefix or suffix will remain
unchanged. An error will occur if \code{prefix} or \code{suffix} is \code{NA}.
}
\note{
Both \code{x} and \code{prefix}/\code{suffix} are translated to UTF-8 before
processing.
}
\examples{
x <- c("x_apple", "x_banana", "x_cherry")
rm.prefix(x, "x_")
rm.suffix(x, "_cherry")
}
\seealso{
\link{startsWith}, \link{endsWith}
}
