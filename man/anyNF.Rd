% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/na.r
\name{anyNF}
\alias{anyNF}
\title{Are any non-finite values present?}
\usage{
anyNF(x)
}
\arguments{
\item{x}{a logical, numeric, or complex vector.}
}
\value{
a single \code{TRUE} or \code{FALSE}.
}
\description{
Tests if a vector contains any non-finite values
(\code{Inf}, \code{-Inf}, \code{NaN}, or \code{NA}).
}
\details{
\code{anyNF()} only does one pass of the input and
returns \code{FALSE} immediately when encountering
a non-finite value, and thus can be much faster
than \code{!all(is.finite(x))}.
}
\note{
\code{anyNF()} will throw an error if the input is not a numeric vector.
For character vectors use \link{anyNA}.
}
\examples{
anyNF(1:10)
anyNF(c(1, 2, NA, 4))
anyNF(c(1, 2, NaN, 4))
anyNF(c(1, 2, Inf, 4))
anyNF(c(1, 2, -Inf, 4))

try(anyNF("1"))
}
\seealso{
\link{is.finite}, \link{is.whole}
}
